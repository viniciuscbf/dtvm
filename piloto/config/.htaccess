# Configuração/segredos — NUNCA acessível via HTTP (contém credenciais do banco).
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
